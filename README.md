The EKF testing file is a Python simulation that measures theoretical SOC vs. actual SOC so that error handling on EKF can be refined before deployment on hardware
The EKF ESP code is code for my ESP32 to gather current sensor data, compute SOC using EKF and do dynamic load shedding depending on SOC
